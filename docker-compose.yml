version: '3'
services:
  api:
    build:
      context: ./app/
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  # Mount Docker socket for Docker in Docker
    ports:
      - "3001:3000"
  web:
    build:
      context: ./web/
    ports:
      - "3000:8080"